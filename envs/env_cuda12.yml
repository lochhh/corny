name: pytorch_cuda12
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python
  - pip
  - ipywidgets
  - pytorch
  - torchvision
  - pytorch-cuda=12.1
  - ultralytics
  - lightning
  - pip:
      - ipykernel
      - supervision
      - segmentation-models-pytorch
      - tensorboardX
      - tensorboard
      - albumentations